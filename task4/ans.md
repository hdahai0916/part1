# task4
## ans
项目结构说明：
part1是整个项目根目录，task4.com.example是HelloWold.java所属的包，HelloWold.java是task4的入口类，它的main函数负责启动程序，其中import了task4.com.example.tool包里的print类，并调用了Print类的print函数。tool是task4.com.example的子包，包含工具类，其中的Print.java定义了print类，Print类提供了打印的函数。